package com.divaneural.argus

// Placeholders para compilar no CI (sem segredos)
fun getAdminUser(): String = ""   // ajuste depois no app (tela de config)
fun getAdminPass(): String = ""   // ajuste depois no app (tela de config)

// Endpoint padrão da Diva Core (pode mudar na tela de config)
fun getDefaultApiBase(): String = "https://divaneural.com"
